{
    "builds": [
      {
        "src": "FOODZONE/wsgi.py",
        "use": "@vercel/python",
        "config": {
            "buildCommand": "build_file.sh"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "FOODZONE/wsgi.py"
      }
    ]
}